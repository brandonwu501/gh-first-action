name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs_on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo 'Hello!'
      - name: Print bye
        run: echo 'Bye!'
